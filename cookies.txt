# Netscape HTTP Cookie File
# https://curl.se/docs/http-cookies.html
# Placeholder cookie jar. libcurl rewrites this at runtime (CURLOPT_COOKIEFILE/JAR).
# Never commit real session data — keep only this header in version control.
